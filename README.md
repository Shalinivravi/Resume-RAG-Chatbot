# 📄 AI Resume Screening Chatbot

A powerful RAG-based application to help recruiters screen multiple resumes using natural language.

## 🚀 Features
- **Bulk Upload**: Upload multiple PDFs to analyze simultaneously.
- **Smart Chat**: Ask questions about candidates (skills, experience, etc.).
- **JD Matching**: Paste a Job Description and get a ranked list of best-fit candidates.
- **Context-Aware**: Remembers the conversation for follow-up questions.

## 🛠️ Setup
1. Clone the repository.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Set your `GOOGLE_API_KEY` in a `.env` file.
4. Run the app:
   ```bash
   streamlit run app.py
   ```

## 🌐 Deployment
To get a public link, deploy this repository to **Streamlit Cloud**.
